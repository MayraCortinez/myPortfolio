---
import { getCollection } from "astro:content";
import { getUniqueTags } from "../../../utils/getUniqueTags";
const projects = await getCollection('projects');
const uniqueTags = await getUniqueTags(projects);
const displayedTags = uniqueTags.slice(0, 5);
---

<ul class="flex-column backdrop-blur-2xl p-2 text-left rounded-lg">
  {displayedTags.map(tag => (
    <li>
      <a  data-astro-reload href={`/tags/${tag}`} class="text-white hover:text-orange-400">{tag}</a>
    </li>
  ))}

    <a data-astro-reload href="/tags" class="text-white hover:text-orange-400 font-bold pt-4">Ver más</a>

</ul>